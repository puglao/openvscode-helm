apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openvscodeserver-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "openvscodeserver-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openvscodeserver-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
